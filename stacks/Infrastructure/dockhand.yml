services:
  dockhand:
    image: 'fnsys/dockhand:v1.0.21'
    container_name: dockhand
    restart: unless-stopped
    environment:
      - SERVICE_FQDN_DOCKHAND_3000
    volumes:
      - '/var/run/docker.sock:/var/run/docker.sock'
      - '/data/dockhand:/app/data'
